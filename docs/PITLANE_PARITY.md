# Pitlane route and capability parity

Checked 11 September 2026. The 53 entries below map to production components and backend handlers. “Fixture verified” means the listed operation/check ran against isolated data; it does not assert real simulator or external WHEP interoperability. Backend paths below are prefixed by `/api`.

## Advanced: 53 tools / 14 sections

| Section | Tool | Destination / role | Production component and handler | Validation evidence | Status |
| --- | --- | --- | --- | --- | --- |
| Fix a problem | Check why a server won’t start | `/setup` · admin | SetupWorkbench.vue; `GET /setup/summary, /server/readiness` | Readiness/API checks; setup and exact launch regressions | Fixture verified; external limits below |
| Fix a problem | Drivers are connected, but the map is empty | `/server` · viewer | ServerDetail.vue, Broadcast.vue; `/server/*?instance=N; SSE` | Browser timing/map; independent process, restart and ownership regressions; game commands require a real client | Fixture verified; external limits below |
| Fix a problem | A stream is missing or won’t record | `/settings/streams` · admin | StreamsDebug.vue; `GET /streams/debug; POST /streams/debug/probe` | Actual ffmpeg recorder, segment freshness, JPEG/clip tests | Fixture verified; external limits below |
| Fix a problem | A car or track is missing | `/garage/content` · admin | Content.vue, ContentDetail.vue; `/content/*, /car/*, /track/*, /weather/*` | Real ZIP failure/retry/overwrite; recache, metadata/archive/undo, guarded deletion; source/URL-job audit | Fixture verified; external limits below |
| Installation & engine | Installation & discovery | `/settings/installation` · admin | InstallationSettings.vue; `/config/content; /validate/installpath` | Fixture installation/config save and real content recache | Fixture verified; external limits below |
| Installation & engine | Dedicated server engine | `/settings` · admin | SettingsConfig.vue; `GET/PUT /config` | Fixture configuration round trip; actual simulator engines external | Fixture verified; external limits below |
| Installation & engine | Custom Shaders Patch | `/settings/installation` · admin | InstallationSettings.vue; `/config/content; /validate/installpath` | Fixture installation/config save and real content recache | Fixture verified; external limits below |
| Server configuration | Lobby identity & joining | `/settings` · admin | SettingsConfig.vue; `GET/PUT /config` | Fixture configuration round trip; actual simulator engines external | Fixture verified; external limits below |
| Server configuration | Joining rules & server passwords | `/settings` · admin | SettingsConfig.vue; `GET/PUT /config` | Fixture configuration round trip; actual simulator engines external | Fixture verified; external limits below |
| Server configuration | Limits & network behaviour | `/settings` · admin | SettingsConfig.vue; `GET/PUT /config` | Fixture configuration round trip; actual simulator engines external | Fixture verified; external limits below |
| Servers & ports | Edit server & port assignments | `/settings/instances` · admin | SettingsInstances.vue; `/instances/*` | Create/edit/delete, port collision, repeat/schedule; two running fixture processes isolated | Fixture verified; external limits below |
| Servers & ports | Add an independent server | `/settings/instances` · admin | SettingsInstances.vue; `/instances/*` | Create/edit/delete, port collision, repeat/schedule; two running fixture processes isolated | Fixture verified; external limits below |
| Servers & ports | Remove a server | `/settings/instances` · admin | SettingsInstances.vue; `/instances/*` | Create/edit/delete, port collision, repeat/schedule; two running fixture processes isolated | Fixture verified; external limits below |
| Servers & ports | Reserve a spectator slot | `/settings/streaming` · admin | StreamingCapture.vue; `/config; /driver-streams; /instances/*` | Capture settings retained; real recorder tested; spectator renderer regressions | Fixture verified; external limits below |
| Servers & ports | Server-specific driving rules | `/settings/instances` · admin | SettingsInstances.vue; `/instances/*` | Create/edit/delete, port collision, repeat/schedule; two running fixture processes isolated | Fixture verified; external limits below |
| Custom sessions | Build a completely custom session | `/sessions/new` · steward | SessionEditor.vue; `/driving-sessions/*` | Whole values, immutable setup, preview, durable exact launch, concurrent retry and failure recovery | Fixture verified; external limits below |
| Custom sessions | Organise setups & groups | `/events` · admin | Events.vue; `/events, /event/*, /categories` | Existing user-flow tests; shared/private scope audit; all queue/start entry points use exact session review | Fixture verified; external limits below |
| Custom sessions | Inspect or customise generated files | `/sessions/new` · steward | SessionEditor.vue; `/driving-sessions/*` | Whole values, immutable setup, preview, durable exact launch, concurrent retry and failure recovery | Fixture verified; external limits below |
| Custom sessions | Operate a running session | `/server` · steward | ServerDetail.vue, Broadcast.vue; `/server/*?instance=N; SSE` | Browser timing/map; independent process, restart and ownership regressions; game commands require a real client | Fixture verified; external limits below |
| Custom sessions | Change a running setup | `/server` · steward | ServerDetail.vue, Broadcast.vue; `/server/*?instance=N; SSE` | Browser timing/map; independent process, restart and ownership regressions; game commands require a real client | Fixture verified; external limits below |
| Presets & scoring | Assists, realism & track grip | `/presets/difficulty` · admin | PresetDifficulty.vue, DifficultyFields.vue; `/difficulties, /difficulty/*` | HTTP create/read/save/delete; immutable ordinary session values; shared usage confirmation | Fixture verified; external limits below |
| Presets & scoring | Booking, practice, qualifying & race | `/presets/sessions` · admin | PresetSession.vue, SessionFields.vue; `/sessions, /session/*` | HTTP create/read/save/delete; phase rendering regressions | Fixture verified; external limits below |
| Presets & scoring | Time & ordered weather panels | `/presets/time` · admin | PresetTime.vue, TimeFields.vue; `/times, /time/*` | HTTP create/read/save/delete; ordered weather/CSP and whole-value validation | Fixture verified; external limits below |
| Presets & scoring | Car classes, liveries & ballast | `/presets/classes` · steward | PresetClass.vue, ClassFields.vue; `/classes, /class/*` | HTTP create/read/save/delete; grid/skin/capacity and isolation regressions | Fixture verified; external limits below |
| Presets & scoring | Drift scoring modes | `/presets/drift-scoring` · admin | DriftScoringModes.vue; `/drift-scoring-modes, /drift-scoring-mode/*` | HTTP create/read/save/delete; scoring regressions; independent duplicate action | Fixture verified; external limits below |
| Presets & scoring | Manage reusable presets | `/presets` · steward | PresetTemplates.vue, usePresetPage.ts; `All five preset CRUD families` | All five HTTP CRUD families; usage and role audit | Fixture verified; external limits below |
| Queue & automation | Edit the running order | `/queue` · steward | Queue.vue; `/queue/*, /instances/:id/runmode` | Existing queue tests; exact launch/head, repeat freeze, due-plan overrun and cancellation regressions | Fixture verified; external limits below |
| Queue & automation | Repeat a session | `/queue` · steward | Queue.vue; `/queue/*, /instances/:id/runmode` | Existing queue tests; exact launch/head, repeat freeze, due-plan overrun and cancellation regressions | Fixture verified; external limits below |
| Queue & automation | Scheduled starts & boot behaviour | `/settings/instances` · admin | SettingsInstances.vue; `/instances/*` | Create/edit/delete, port collision, repeat/schedule; two running fixture processes isolated | Fixture verified; external limits below |
| Streams & recording | Manage every stream source | `/garage/rigs` · admin | Rigs.vue, ScreenSetupArt.vue; `/rigs, /sources, /sources/status` | Browser Add/edit; revision/duplicate tests; idle playback and real capture | Fixture verified; external limits below |
| Streams & recording | Photos, clips & automatic highlights | `/settings/streaming` · admin | StreamingCapture.vue; `/config; /driver-streams; /instances/*` | Capture settings retained; real recorder tested; spectator renderer regressions | Fixture verified; external limits below |
| Streams & recording | Recorder health, buffers & probes | `/settings/streams` · admin | StreamsDebug.vue; `GET /streams/debug; POST /streams/debug/probe` | Actual ffmpeg recorder, segment freshness, JPEG/clip tests | Fixture verified; external limits below |
| Streams & recording | Source status & advanced links | `/garage/rigs` · admin | Rigs.vue, ScreenSetupArt.vue; `/rigs, /sources, /sources/status` | Browser Add/edit; revision/duplicate tests; idle playback and real capture | Fixture verified; external limits below |
| Content maintenance | Browse all content | `/garage/content` · admin | Content.vue, ContentDetail.vue; `/content/*, /car/*, /track/*, /weather/*` | Real ZIP failure/retry/overwrite; recache, metadata/archive/undo, guarded deletion; source/URL-job audit | Fixture verified; external limits below |
| Content maintenance | Archive & URL imports | `/garage/content` · admin | Content.vue, ContentDetail.vue; `/content/*, /car/*, /track/*, /weather/*` | Real ZIP failure/retry/overwrite; recache, metadata/archive/undo, guarded deletion; source/URL-job audit | Fixture verified; external limits below |
| Content maintenance | Inspect complete content metadata | `/garage/content` · admin | Content.vue, ContentDetail.vue; `/content/*, /car/*, /track/*, /weather/*` | Real ZIP failure/retry/overwrite; recache, metadata/archive/undo, guarded deletion; source/URL-job audit | Fixture verified; external limits below |
| Content maintenance | Rebuild the content index | `/garage/content` · admin | Content.vue, ContentDetail.vue; `/content/*, /car/*, /track/*, /weather/*` | Real ZIP failure/retry/overwrite; recache, metadata/archive/undo, guarded deletion; source/URL-job audit | Fixture verified; external limits below |
| Content maintenance | Remove content from disk | `/garage/content` · admin | Content.vue, ContentDetail.vue; `/content/*, /car/*, /track/*, /weather/*` | Real ZIP failure/retry/overwrite; recache, metadata/archive/undo, guarded deletion; source/URL-job audit | Fixture verified; external limits below |
| People & access | Accounts & roles | `/settings/users` · admin | Users.vue; `/users/*` | Viewer/steward/admin HTTP checks; password reset, role update and delete | Fixture verified; external limits below |
| People & access | Reset an operator password | `/settings/users` · admin | Users.vue; `/users/*` | Viewer/steward/admin HTTP checks; password reset, role update and delete | Fixture verified; external limits below |
| Your preferences | Measurement & temperature units | `/preferences` · viewer | SettingsUser.vue, account sheet; `GET/PUT /user` | HTTP unit round trip; role/password checks; browser day/night controls | Fixture verified; external limits below |
| Your preferences | Your password & account | `/preferences` · viewer | SettingsUser.vue, account sheet; `GET/PUT /user` | HTTP unit round trip; role/password checks; browser day/night controls | Fixture verified; external limits below |
| History & attribution | Find a driving stint or result | `/sessions/drives` · viewer | SessionSearch.vue, ResultsHistory.vue; `/driver-sessions, /results/files` | Legacy filters/query redirect; actual fixture stints and result file | Fixture verified; external limits below |
| History & attribution | Driver profiles, progress & media | `/drivers` · viewer | DriverStats.vue, DriverDetail.vue, GuestDriverDetail.vue; `/drivers/*, /guest-drivers/*, /scores/*` | Real protocol history, handover/pinned media, guest profile and role tests | Fixture verified; external limits below |
| History & attribution | Correct who was driving | `/drivers` · admin | DriverStats.vue, DriverDetail.vue, GuestDriverDetail.vue; `/drivers/*, /guest-drivers/*, /scores/*` | Real protocol history, handover/pinned media, guest profile and role tests | Fixture verified; external limits below |
| History & attribution | Tags & captured media | `/drivers` · steward | DriverStats.vue, DriverDetail.vue, GuestDriverDetail.vue; `/drivers/*, /guest-drivers/*, /scores/*` | Real protocol history, handover/pinned media, guest profile and role tests | Fixture verified; external limits below |
| History & attribution | Club records & spectator display | `/leaderboard` · viewer | Leaderboard.vue, BroadcastLeaderboard.vue; `/scores; shared SSE` | Score/telemetry/frontend ranking tests; real protocol laps and capture links | Fixture verified; external limits below |
| Data & recovery | Back up the club | `/maintenance` · admin | Maintenance.vue; `/server/smdata, /server/smcontent, /maintenance/restore` | Consistent HTTP backup; corrupt-stage preservation and valid apply/rollback unit test | Fixture verified; external limits below |
| Data & recovery | Stage a database restore | `/maintenance` · admin | Maintenance.vue; `/server/smdata, /server/smcontent, /maintenance/restore` | Consistent HTTP backup; corrupt-stage preservation and valid apply/rollback unit test | Fixture verified; external limits below |
| Data & recovery | Clean up failed jobs & temporary files | `/garage/recovery` · admin | Recovery.vue; `/recovery/cleanup, /recovery/changes/*` | Preview-token cleanup and optimistic metadata undo: HTTP and regression tests | Fixture verified; external limits below |
| System & logs | Version, paths & support information | `/about` · viewer | About.vue; `/about; /server/logfile` | HTTP version/log/archive; privileged download role audit | Fixture verified; external limits below |
| System & logs | Application logs & telemetry details | `/server` · viewer | ServerDetail.vue, Broadcast.vue; `/server/*?instance=N; SSE` | Browser timing/map; independent process, restart and ownership regressions; game commands require a real client | Fixture verified; external limits below |
| System & logs | Recent changes & undo | `/garage/recovery` · admin | Recovery.vue; `/recovery/cleanup, /recovery/changes/*` | Preview-token cleanup and optimistic metadata undo: HTTP and regression tests | Fixture verified; external limits below |

## Complete original surfaces

The original detailed editors remain production components inside the Pitlane shell. They share the existing API client, confirmation/unsaved-change primitives, content cache, and SSE recovery. They are not links to the concept.

| Existing route family | Production home / compatibility |
| --- | --- |
| `/`, `/login`, `/preferences`, `/about`, `/access-denied` | Today and account menu; redirect-back authentication, units/password, appearance, build information |
| `/server/:id`, `/server/:id/broadcast`, `/broadcast` | Garage → Your servers; session control and Watch; instance IDs stay instance IDs |
| `/sessions?q=…&tag=…` | Redirects to `/sessions/drives`, carrying all query parameters and hash |
| `/sessions/drives`, `/history`, `/drivers/:guid`, `/guest-drivers/:id` | Sessions history and Drivers; connection, stint, result and person identities preserved |
| `/leaderboard` | Garage → The club → Club records; complete display and filtering controls |
| `/events`, `/queue`, `/presets/*`, `/setup` | Sessions secondary tools and Garage → Advanced; old queue-and-start entry points now open isolated review |
| `/content` | Alias for `/garage/content`; new car/track/weather management details |
| `/settings`, `/settings/installation`, `/settings/instances`, `/settings/streaming`, `/settings/streams`, `/settings/users`, `/maintenance`, `/admin` | Garage server/club sections and Advanced; same backend permissions |
| `/app/*` | Go 301 redirect preserves the raw query; root SPA deep links serve the embedded build |

## Original controls audited beyond catalogue titles

The source audit includes every original editor field and action: installation validation, engine/CSP settings, content download URL, lobby passwords and locked entry lists, network/client/thread limits, all five instance ports, start-on-boot and repeat/manual mode, spectator slot identity/car/skin, drift HUD/wrong-way rules, reusable group and setup operations, grid counts/ballast/skins, all assists/grip/voting settings, booking/qualifying/race join rules and pit windows, ordered weather/CSP time/date, scoring weights/reset timers, queue reorder/category enqueue/skip/clear, race commands and current setup restart choice, recording thresholds/cooldowns/limits, source diagnostics and probes, content batches/URL jobs/compression/downloads, account last-admin protection, personal units/password, avatars, history tags/deletion/correction, result files, clip playback/download/delete and guest assignment.

## External verification limits

- The available AC dedicated-server binary is x86; this Docker environment is ARM64. A labelled UDP protocol fixture exercises real process launch, telemetry ingestion, exact result filenames and history. Kunos/AssettoServer with a real game client, CSP negotiation, spectator client connection and in-game admin effects remain external checks.
- Actual local video/embed playback, ffmpeg MPEG-TS recording, JPEG snapshots and MP4 encoding were verified. No external MediaMTX/WHEP endpoint or physical club camera was provided; those end-to-end transports remain external checks.
- Actual map display was checked with the installed Drift map; projection/ranking/interpolation tests cover numerical behavior. A real driving survey across other installed layouts remains an external game check.
- Native fullscreen and panel measurements passed all four requested sizes. The in-app browser raster capture crops/stitches fixed content above the physical window; the delivered fullscreen image is the valid 1440 CSS-pixel view, and the 4K evidence is recorded geometry/native state.

See `PITLANE_IMPLEMENTATION_PROGRESS.md`, `PITLANE_MIGRATION.md`, and `pitlane-integration-results.json` for exact commands, results and reproduction.
