Production SPA assets are generated here by `npm run build` from `webapp/`.

The generated `dist/` directory is ignored, but this tracked file keeps the
embedded webapp root present so Go tooling can load the package after a clean.
